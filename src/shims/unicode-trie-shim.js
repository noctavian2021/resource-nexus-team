
// This is a browser shim for unicode-trie module
import unicodeTrie from 'unicode-trie';

// Re-export as both default and named export for compatibility
export default unicodeTrie;

// Re-export any other named exports if needed
