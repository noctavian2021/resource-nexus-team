
// This is a browser shim for the clone module
import clone from 'clone';

// Re-export clone as both default and named export for compatibility
export default clone;

// Re-export any other named exports if needed
